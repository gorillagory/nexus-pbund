services:
  db:
    image: postgres:15-alpine
    container_name: nexus-db
    environment:
      POSTGRES_USER: nexus
      POSTGRES_PASSWORD: agenticmesh
      POSTGRES_DB: nexus_os
    ports:
      - "127.0.0.1:5434:5432"
    volumes:
      - "./.postgres-data:/var/lib/postgresql/data"
